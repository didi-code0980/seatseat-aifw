# state enum: IDEA TRIAGE BACKLOG SPEC READY DESIGN IN_PROGRESS REVIEW QA REWORK ESCALATED DONE
#
# Seeded from .ai/templates/ticket.yaml on 2026-08-26 by the orchestrator, after the operator chose
# GRP-02 over REG-01 to restart the loop.
#
# THE TICKET DOES NOT PREDATE ITS ROW, AND THIS IS THE FIRST ONE FOR WHICH THAT WAS ALREADY TRUE.
# `.ai/registry/features.md` carries `GRP-02 | PLANNED` with an ID, merged in PR #44 before this file
# existed. GRP-01's own header records the opposite case — it was seeded by the `ba` at SPEC against
# an empty GRP table, and check D1 failed on every document that cited it. REG-01 was seeded the same
# way. Nothing here needs a human to catch up.
#
# WHY THIS ONE AND NOT REG-01. REG-01's row carries a 🟡 — whether self-release and the request
# workflow are one feature or two — and that question blocks READY, so `/spec` would run and stop at
# the DoR gate. GRP-02 owes no decision: scope, deletion semantics and the deferral were all settled
# during GRP-01, by the operator, in writing.
#
# THE SCOPE IS TRANSCRIBED, NOT COMPOSED. Every clause below is quoted from a merged artifact:
#   - `01-story.md:376` out-of-scope item 1 — "Putting a member into a group, and taking them out.
#     The member side of *a Member belongs to a Group*. Deferred to `GRP-02` by name, in the feature
#     row itself." A surface that grows a *move this person to another group* control is this ticket.
#   - `01-story.md:383` out-of-scope item 2 — "Any group-scoped view of members. *The members of this
#     group*, a count on a group row, a filter on the member list, or a group column returning to the
#     member screen." It records that item 2 needs item 1 first, because a group-scoped view is empty
#     until members can be put into groups. Both items are inside THIS ticket; the ordering is
#     internal to it and is not a dependency on another ticket.
#   - `02-design.md:435` — "The ticket that restores a group column to `/members` — GRP-02,
#     out-of-scope items 1 and 2 — makes `/members` a second reader and must add the path in the same
#     change." That is a design obligation this ticket inherits: `revalidatePath("/members")` beside
#     the existing `/groups` call. `src/actions/groups.ts:16` carries the same note in the code.
#
# ONE THING SPEC INHERITS AND MUST NOT INVENT. MEM-01 dropped the group column deliberately, and its
# reason was NOT cosmetic: *"a group id is not a group name and no seam function resolves one"*
# (`02-design.md:432`). Restoring the column therefore needs a resolver that does not exist yet.
# Whether that is a new seam function, a join in `listMembers`, or a shape change to the member DTO is
# DESIGN's, not SPEC's — but the story has to state that the column cannot be restored by rendering
# `Member.groupId`, or DESIGN will discover it after `allowed_paths` is fixed.
#
# ALSO OPEN IN THE REGISTRY AND STILL NOT THIS TICKET'S TO CLOSE — the same two GRP-01 refused to
# answer: maximum nesting depth (`.ai/registry/glossary.md`), and whether a Member may belong to more
# than one Group (`prisma/schema.prisma` models one, and says so as the narrower reading). The second
# one presses harder here than it did on GRP-01, because assignment is exactly where a second group
# would have to be expressible. If the story needs it answered, it goes under OPEN QUESTIONS and
# routes to a human — it is not chosen.
#
# --- APPENDED BY `ba` AT SPEC, 2026-08-26T03:07:43Z. Nothing above this line was changed. ---
#
# THE THREE FIELDS BELOW THAT MOVED ARE `state`, `size_estimate` AND `gates.spec`, plus `branch`.
# `invariants_touched` did not move and the header above predicted correctly that it would not; the
# BA's own per-ID reasoning is in `01-story.md` and the comment on that field records the verdict
# without replacing the orchestrator's provisional note.
#
# BOTH THINGS THE HEADER ABOVE TOLD SPEC TO DO ARE DONE, and each is checkable:
#   - *the story has to state that the column cannot be restored by rendering `Member.groupId`* —
#     `01-story.md` says it under **User value**, under **Size** as the reason the estimate is not
#     `S`, and again as `Q-1`, which routes the resolver question to DESIGN and gates `/qa`.
#   - *whether a Member may belong to more than one Group ... is not chosen* — it is `Q-5`, routed to
#     the operator. `AC-7` writes the glossary's narrower reading (*a Member belongs to a Group*) as
#     a criterion, and `Q-5` states plainly that AC-7 is withdrawn if the answer is many-to-many.
#     The criterion exists so the ticket is buildable; the question exists so the choice stays human.
#
# ONE THING THE HEADER ABOVE COULD NOT HAVE KNOWN, recorded because it is a fact about this run
# rather than about the ticket: the first `/spec GRP-02` returned `gate: BLOCKED` claiming no ticket
# existed. That was wrong. This worktree was parked on a stale `origin/main` that predated 6ba81f4,
# so the file was invisible rather than absent, and the branch for this run is cut from the current
# `origin/main` where it is present. No artifact carries the mistaken claim; it is written here
# because a reader comparing the chat to the repository would otherwise find a contradiction and no
# explanation.
id: GRP-02
title: "Member assignment to groups"
feature_ids: [GRP-02]
group: GRP
state: IN_PROGRESS   # ADVANCED FROM SPEC BY /design ON 2026-08-26T03:28:58Z, gate PASS.
                     # THE TICKET NEVER SAT AT READY, and that is a fact about how it was run rather
                     # than a defect: the operator invoked /design directly, so the orchestrator's
                     # DoR evaluation was not performed. GRP-01 was advanced the same way and its
                     # header says so. The four DoR items the story carried all hold and are
                     # checkable here — `feature_ids` resolves to `.ai/registry/features.md:129`,
                     # `invariants_touched` is explicit, out-of-scope is non-empty at twelve items,
                     # and `size_estimate` is set. The DESIGN gate is what this transition rests on.
                     #
                     # ---- THE RECORD THIS REPLACES, KEPT ----
                     # state: SPEC. ADVANCED FROM BACKLOG BY /spec ON 2026-08-26T03:07:43Z, gate PASS.
                     # DoR is the orchestrator's to evaluate and this stage does not set READY.
owner: ""
branch: "feat/GRP-02"   # cut from origin/main by /spec step 0, 2026-08-26.
size_estimate: "M"    # SET BY ba AT SPEC. Grounds are in `01-story.md` under **Size**, and the
                      # short form is that this is two halves rather than one field: a write path
                      # that does not exist today, and a column MEM-01 removed *because no seam
                      # function resolves a group id to a group name*. AC-1 requires the name, so
                      # that resolution is the work. Not `S` for that reason; not larger than `M`
                      # because the tree is built, the field exists, schema_delta is none and no
                      # invariant is engaged.
size: "M"             # VERDICT, 2026-08-26T03:28:58Z, counted from the ten paths enumerated
                      # below against the sizing table in `.ai/01-operating-model.md` — M is up to 12.
                      # IT AGREES WITH `size_estimate` AND NOTHING ROUTES BACK TO `ba`.
                      #
                      # NOT XL, and the row that would say otherwise is read with the paragraph under
                      # it. The table's XL row names `types.ts`, which this ticket changes; the
                      # paragraph fixes the test as "whether existing callers must change, not whether
                      # the seam was touched at all", and adds that adding new functions to
                      # `src/lib/data/` is ordinary feature work. One type is ADDED beside `Member`,
                      # which does not change; one seam function is ADDED; no existing signature
                      # moves and no existing caller is affected. GRP-01 and MEM-01 are both worked
                      # precedents, additive in the same file and both sized M.
                      #
                      # THE ONE ALTERNATIVE THAT WOULD HAVE BEEN XL is recorded because it is the
                      # obvious-looking one: putting `groupId` into `MemberPatch` and the existing
                      # edit form changes the signature of a shipped seam function, which the table
                      # sends to XL and to escalation. 02-design.md section 7 alternative A.
depends_on: [GRP-01]  # SATISFIED — GRP-01 is DONE and merged as PR #42 on 2026-08-26T02:49:03Z, so
                      # DoR item 3 passes. Recorded rather than left empty because it is true and
                      # checkable: members cannot be assigned to groups until groups can be created,
                      # and GRP-01 is the ticket that made them creatable. Every ticket this project
                      # has issued carried `[]` here, which made DoR item 3 vacuous six times running.
invariants_touched: []   # PROVISIONAL, AND IT MATCHES THE REGISTRY ROW RATHER THAN ASSERTING MORE
                         # THAN IT. That row says `[]` and says why: "nothing in invariants.md
                         # mentions Group, but member assignment writes MEM's surface, and SPEC
                         # determines the real set." The `ba` re-checks and does not transcribe —
                         # SEA-01 went from four to seven at SPEC, MEM-01 from four to two. INV-12 is
                         # the one worth reasoning through explicitly: it governs member DELETION, and
                         # nothing here deletes a member, but the file this ticket writes is the file
                         # INV-12 protects.
                         #
                         # CONFIRMED `[]` BY ba AT SPEC, 2026-08-26 — no longer provisional. All
                         # eleven issued IDs were walked one at a time, not dismissed as a class, and
                         # the table is in `01-story.md` under **Invariants touched**. INV-12 came
                         # out where this note predicted. The finding that makes `[]` an argument
                         # rather than an assumption is the absence of a cascade: INV-11's note is
                         # the worked example of an invariant reached invisibly, and the equivalent
                         # chain does not exist here because the glossary states group membership is
                         # independent of seat occupancy — so there is no relation from a Group to a
                         # Seat, a Device or a Room for one to travel along.
allowed_paths:           # SET BY tech-lead-design AT DESIGN, 2026-08-26T03:28:58Z. Ten files,
                         # enumerated and not globbed. 02-design.md section 5 carries the reason each
                         # one is in reach, and — as load-bearing — the six a reader might expect and
                         # which are deliberately absent. Two of those six were checked rather than
                         # assumed: `tests/e2e/members.spec.ts` addresses every cell by testid and
                         # counts no columns, so the new Group column does not break it (F-7).
  - "src/app/(app)/members/page.tsx"
  - "src/app/(app)/members/members-manager.tsx"
  - "src/actions/members.ts"
  - "src/actions/groups.ts"
  - "src/lib/validation/member.ts"
  - "src/lib/data/types.ts"
  - "src/lib/data/mock/members.ts"
  - "src/lib/data/prisma/members.ts"
  - "tests/unit/member-groups.test.ts"
  - "tests/e2e/member-groups.spec.ts"
                         #
                         # `src/actions/groups.ts` IS THREE LINES AND IT IS NOT SCOPE DRIFT. GRP-01
                         # wrote the obligation into that file's own header: "/members is
                         # deliberately not revalidated ... The ticket that restores a group column
                         # to /members — GRP-02 — makes /members a second reader and must add the
                         # path in the same change." All three group write actions gain
                         # `revalidatePath("/members")`, not only the delete — a rename changes the
                         # text the new column renders, and a create adds an option to a chooser
                         # /members renders. That last one is MEM-01's finding F-6, which QA
                         # measured rather than predicted.
                         #
                         # `src/lib/validation/group.ts` IS ABSENT ON PURPOSE and it is the path most
                         # likely to be reached for at IN_PROGRESS. Its `groupParentIdSchema` maps
                         # the empty string to null, which on this surface would silently build the
                         # "remove from group" verb the story declined to grant (Q-4, out-of-scope
                         # item 2) with no criterion to fail. 02-design.md F-4 and section 7
                         # alternative E.
schema_delta: none    # `Member.groupId` is already drafted and already written by GRP-01's delete
                      # path, so assignment adds no field. If DESIGN concludes the group column needs
                      # a DTO or schema change to resolve a group NAME, that is a finding to raise —
                      # not a decision to take (SYS-01 set this precedent and held `none` throughout).
                      #
                      # HELD AT `none` BY tech-lead-design AT DESIGN, AND VERIFIED RATHER THAN
                      # ASSUMED. `prisma/schema.prisma:166-167` declares `groupId String?` and the
                      # relation with `onDelete: SetNull`, and line 180 an `@@index([groupId])`.
                      # The conditional this note raised did not fire: resolving a group id to a
                      # group NAME needs no DTO and no schema change — `listGroups()` already exists
                      # from GRP-01, and the members page composes it into a name map the way it
                      # already composes three other seam reads. No seam READ is added at all.
                      # 02-design.md F-1 and section 4.
requires_adr: false
rework_count: 0
chat_budget:
  developer->tech-lead-design: { used: 0, max: 6 }
  developer->ba:               { used: 0, max: 6 }
  qa->ba:                      { used: 0, max: 6 }
  qa->tech-lead-design:        { used: 0, max: 6 }
tracker: { provider: clickup, external_id: "", sync_enabled: false }
gates:
  spec:   { passed: true,  at: "2026-08-26T03:07:43Z" }
  design: { passed: true,  at: "2026-08-26T03:28:58Z" }
  review: { passed: false, at: null }
  qa:     { passed: false, at: null }

# Notes on fields that are easy to get wrong:
#
# invariants_touched  [] means "considered, none engaged". Absent means nobody looked.
#                     Definition of Ready requires it to be explicit.
# allowed_paths       [] is a control, not a placeholder. guard-allowed-paths.mjs blocks every
#                     write outside this ticket folder while it is empty. DESIGN fills it.
# rework_count        Only Developer-caused failures increment it (RULE-08). Two failures
#                     escalate (RULE-06).
# chat_budget         Six messages per pair per ticket (RULE-15). chat-guard.mjs increments
#                     `used` and blocks at `max`. Pairs absent from this map are not chattable.
# sync_enabled        false by default (RULE-10). Turn on per ticket, after the loop is proven.
