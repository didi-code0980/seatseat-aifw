# state enum: IDEA TRIAGE BACKLOG SPEC READY DESIGN IN_PROGRESS REVIEW QA REWORK ESCALATED DONE
# Seeded from .ai/templates/ticket.yaml at 2026-08-26, after the operator answered ADR-007's four
# remaining open questions with a technical lead and the SYS-02 row was written.
id: SYS-02
title: "Cutover to Supabase as the data client"
feature_ids: [SYS-02]
group: SYS
state: IN_PROGRESS   # RETURNED FROM THE SECOND QA FAIL BY /design ON 2026-08-28T02:06:22Z, gate PASS.
                     #
                     # THE ROUTE DID NOT ARRIVE HERE FIRST. `06-test-report.md` gated FAIL on AC-1
                     # having no named test and routed to `ba` on the `R6` row; `ba` amended
                     # `01-story.md` at 2026-08-27T07:56:34Z, re-PASSED SPEC, and set
                     # `next_state: DESIGN` with one constraint attached. This stage executed that
                     # constraint. `02-design.md` section 6.4 is the `pnpm test:db` lane, and section
                     # 5 gains SIX paths for it: `tests/db/**`, `tests/db-e2e/**`,
                     # `scripts/test-db.mjs`, `scripts/local-stack-client.ts`, `vitest.db.config.mts`
                     # and `playwright.db.config.ts`.
                     #
                     # WHAT IS OWED AT IN_PROGRESS IS THE LANE, NOT THE TESTS. `02-design.md` 5.2
                     # splits the six paths: the developer writes the runner, the two configs, the
                     # `test:db` script, the two CI steps and the `postgres` devDependency, and
                     # writes NOTHING under `tests/db/**` or `tests/db-e2e/**`. QA writes those two
                     # directories and nothing else on the list. RULE-05 is the reason; 5.2 states
                     # it.
                     #
                     # NO `src/**` FILE CHANGES ON THIS CYCLE. Sections 1 to 4 of the design are
                     # unchanged, so the implementation that REVIEW and QA already read is untouched.
                     #
                     # `rework_count` STAYS 0. RULE-08 — the failure was the story's, and
                     # `06-test-report.md` set `increments_rework_count: false` itself.
                     #
                     # `D-1` IS STILL OPEN AND `D-4` JOINS IT. The QA gate is worded twice and the
                     # two do not agree: `.ai/01-operating-model.md:100` says "vitest + playwright
                     # exit 0", which reaches the new lane, and `.claude/commands/qa.md:46` says
                     # "`pnpm test` and `pnpm test:e2e` exit 0", which does not. Section 6.4 is built
                     # so both readings are safe. The residue is that under the command wording the
                     # gate can go green while the lane is red; CI is the backstop, with
                     # `REQUIRE_LOCAL_STACK=1`. Both findings are the steward's.
                     #
                     # ---- WHAT THIS LINE SAID BEFORE, KEPT ----
                     # state: REVIEW — ADVANCED FROM IN_PROGRESS BY /implement ON 2026-08-27T07:17:47Z, gate PASS,
                     # ON THE SECOND RUN OF THIS STAGE. `pnpm typecheck` and `pnpm lint` exit 0 and
                     # **`pnpm test` is now green — 8 files, 160 tests, 0 failed.** The one red test
                     # the first run reported is narrowed, exactly as `02-design.md` §5.1 specifies.
                     #
                     # ONE FILE CHANGED ON THIS CYCLE: `tests/unit/self-signup.test.ts`, four lines.
                     # No `src/**` file moved, so the implementation REVIEW and QA already read is
                     # byte-identical. `03-impl-log.md` §*Rework cycle* is the record.
                     #
                     # `rework_count` STAYS 0 AND THE FIELD BELOW IS UNCHANGED. RULE-08 increments it
                     # only on a Developer-caused failure; this one was reported by the developer in
                     # `99-questions.md` before QA ran, and `06-test-report.md` set
                     # `increments_rework_count: false` itself.
                     #
                     # `D-1` IS STILL OPEN AND STILL BLOCKS A GREEN `pnpm verify` — twelve D6
                     # findings in six human-owned documents under `.ai/registry/**` and
                     # `.ai/standards/**`. Not this ticket's paths; a steward change lands with it.
                     #
                     # ---- WHAT THIS LINE SAID BEFORE, KEPT ----
                     # state: IN_PROGRESS — RETURNED FROM THE QA FAIL BY /design ON 2026-08-27T04:51:28Z, DESIGN
                     # AMENDMENT, gate PASS. One path added to `allowed_paths` below; nothing else
                     # in this design changes.
                     #
                     # `06-test-report.md` returned `gate: FAIL`, `next_state: REWORK`,
                     # `routed_to: tech-lead-design`, `increments_rework_count: false` — the
                     # *R5 impossible as specified* row of `.ai/01-operating-model.md`
                     # §*Failure routing*. This stage is that route. **`rework_count` stays 0**,
                     # RULE-08: only Developer-caused failures increment it, and the developer
                     # reported this one in `99-questions.md` before QA ever ran.
                     #
                     # THE TICKET NEVER SAT AT `REWORK` AND THIS FILE DOES NOT PRETEND IT DID.
                     # `/handoff` step 2a, which transcribes `next_state`, `routed_to` and
                     # `increments_rework_count` out of a verdict's front-matter, landed on `main`
                     # in PR #60 and IS NOT ON THIS BRANCH. The hand-off that released
                     # `feat/SYS-02` at `658ab73` ran steps 3 and 6 by hand, so no step 2a ran and
                     # nobody wrote `REWORK`. RULE-13 forbids `/review` and `/qa` to touch this
                     # file, and no `orchestrator` has run in the implement lane since MD-27. The
                     # steward records the whole deadlock as MD-46.
                     #
                     # WHAT REMAINS IS THE DEVELOPER'S AND IT IS TWO LINES — the SYS-01 AC-3
                     # assertion in `tests/unit/self-signup.test.ts`, narrowed exactly as section 5
                     # of `02-design.md` specifies. REVIEW and QA both run again afterwards, which
                     # is why both gate rows below stay `false`.
                     #
                     # ---- WHAT THIS LINE SAID BEFORE, KEPT ----
                     # state: REVIEW — ADVANCED FROM IN_PROGRESS BY /implement ON 2026-08-27T01:28:07Z, gate PASS.
                     #
                     # `pnpm typecheck` and `pnpm lint` exit 0, every contract item in
                     # `02-design.md` section 1 is implemented, and `03-impl-log.md` lists all 51
                     # files touched. Six deviations are declared in that file rather than left for
                     # R5 to find.
                     #
                     # `pnpm test` IS RED — one test, `tests/unit/self-signup.test.ts`, whose SYS-01
                     # AC-3 assertion ADR-007 clause 2 reverses by decision. That path is NOT in
                     # `allowed_paths`, so RULE-03 refuses the edit and the developer did not make
                     # it, and did not widen this field to grant itself the permission either.
                     # Raised in `99-questions.md`, routed to `tech-lead-design`. It does not block
                     # this gate and it does block a green `pnpm verify`, exactly as `D-1` does.
                     #
                     # ---- WHAT THIS LINE SAID BEFORE, KEPT ----
                     # state: IN_PROGRESS — ADVANCED FROM SPEC BY /design ON 2026-08-26T09:22:00Z, gate PASS.
                     #
                     # THE TICKET NEVER SAT AT `READY` AND THIS FILE DOES NOT PRETEND IT DID. `/spec`
                     # left it at SPEC because DoR is the orchestrator's to evaluate and SPEC does not
                     # set READY; the operator then ran `/design` directly. Recorded rather than
                     # smoothed over: `Q-4` in `01-story.md` is the DoR question that was never put,
                     # and it is a bookkeeping question — ADR-007 is ACCEPTED and `adr:` below now
                     # carries the link DoR item 4 reads.
                     #
                     # ---- WHAT THIS LINE SAID BEFORE, KEPT ----
                     # state: SPEC — ADVANCED FROM BACKLOG BY /spec ON 2026-08-26T08:33:56Z, gate PASS, on the
                     # SECOND run. The first returned BLOCKED on `size_estimate` and that verdict is
                     # kept in `01-story.md` rather than erased. DoR is the orchestrator's to
                     # evaluate; this stage does not set READY.
owner: ""
branch: ""
size_estimate: "M"    # SET BY ba ON THE SECOND RUN, 2026-08-26T08:33:56Z, BY Q-1 EXIT (b).
                      #
                      # READ THIS BEFORE TREATING THE NUMBER AS A MEASUREMENT. The first run left this
                      # field EMPTY and gated BLOCKED, because three documents cannot all hold at once:
                      #   - `.ai/01-operating-model.md` Sizing — XL is "any size, if it changes the
                      #     schema", and XL escalates. `schema_delta` below is not `none`, and both
                      #     this file and the feature row say that is EXPECTED here.
                      #   - Definition of Ready item 5 — `size_estimate` is S or M. No third value.
                      #   - `.ai/registry/features.md:152` — not to be split. The same Sizing table
                      #     independently puts this ticket at L, "more than 12 files", handled as
                      #     "must split at DESIGN".
                      #
                      # The operator reissued `/spec SYS-02` unchanged after that verdict. Under the
                      # standing instruction "an instruction repeated is a decision made" that is the
                      # decision to pass, and exit (b) is the only one of Q-1's three this stage can
                      # execute — (a) amends the operating model under RULE-01, (c) is the refused
                      # verdict. Exit (b) reads the field as its own definition states it: "from the
                      # story's scope and its Out-of-scope section". On SCOPE this ticket is one
                      # operation and `M` is honest. On FOOTPRINT it is not `M` and nothing here
                      # claims it is.
                      #
                      # CONSEQUENCE FOR DESIGN, STATED SO IT IS NOT DISCOVERED: `size` below is
                      # expected to come out XL, and the two fields are expected to DISAGREE. The
                      # rule that a story estimated M which designs out larger was under-specified
                      # DOES NOT APPLY HERE and routing this back to SPEC would produce the same
                      # story again. Q-1 in `01-story.md` is the record and is routed to the steward.

size: "L"             # VERDICT, 2026-08-26T09:22:00Z. UNCHANGED BY THE 2026-08-27 AMENDMENT, which
                      # took the list from 25 globs to 26 — one test file, a two-line edit — AND
                      # UNCHANGED BY THE 2026-08-28 AMENDMENT, which took it from 26 to 32: the six
                      # paths of the `pnpm test:db` lane, none of them under `src/**`. `L` is a
                      # band whose floor is "more than 12", not a count, and neither amendment moves
                      # it. Originally counted from
                      # the 25 globs enumerated in
                      # `02-design.md` section 5 — 13 files deleted, 13 created, 28 modified. Grounds
                      # in full under `02-design.md` **Size verdict**; the three things a reader needs
                      # without opening it:
                      #
                      # 1. NOT XL. `.ai/01-operating-model.md` §Sizing was amended on 2026-08-26
                      #    (PR #54) so a schema change WITH AN APPROVED ADR LINKED is not XL on that
                      #    ground. `adr:` below carries ADR-007, which is ACCEPTED. The other two XL
                      #    clauses do not fire: no existing `src/lib/data/` function changes signature,
                      #    and no DTO in `types.ts` changes shape — that file is in `allowed_paths` for
                      #    comment text only.
                      #
                      # 2. NOT SPLIT. `L` prescribes a split; `.ai/registry/features.md:152` forbids
                      #    one and §Sizing now carries that exception by name. Registry is RULE-01 and
                      #    outranks a handling default.
                      #
                      # 3. `size_estimate` ABOVE SAYS `M` AND THE TWO DISAGREE. The header on that
                      #    field predicted this and said the disagreement rule must not be applied
                      #    mechanically. IT WAS NOT. The ticket is NOT routed back to `ba`: the story
                      #    stated the disagreement in advance and argued it, the operator already
                      #    decided this exact question once by reissuing `/spec SYS-02`, and re-running
                      #    SPEC cannot change a number counted from `allowed_paths`. What the gap is
                      #    evidence of stays open as `D-2` in `02-design.md`, routed to the steward:
                      #    PR #54 fixed the XL row and left DoR item 5 still admitting only S or M.
depends_on: []
# TRANSCRIBED from the features.md row. `ba` owes per-ID reasoning at SPEC and is expected to NARROW
# this list, not extend it.
#
# INV-10 is deliberately absent and must not be added back at SPEC. It needs btree_gist and a
# generated column, nobody has chosen the shape, and it stays enforced at the seam. Carried as debt.
# EXTENDED BY `ba` AT SPEC, 2026-08-26T08:03:28Z, FROM THREE IDS TO TEN — against the expectation
# stated four lines above, which said NARROW and not extend. Per-ID grounds are in `01-story.md`
# under **Invariants touched** and are not repeated here.
#
# The reason in one sentence: this is the ticket that authors the FIRST MIGRATION, and a migration is
# the artefact in which every structural invariant is either expressed or silently omitted. The three
# IDs on the feature row are the three that get a named INSTRUMENT in ADR-007 OQ-5; they are not the
# three the change could AFFECT, which is the test `.ai/registry/invariants.md` sets.
#
# The four most easily missed, because each is held by an ABSENCE that a tidy first migration adds:
#   INV-02  held by there being NO unique constraint on the occupant reference
#   INV-07  held by the device owner reference being NULLABLE
#   INV-03  the invariant's own note names this ticket twice: no migration may add a `status` column
#           to the seat table, and no seed may write one
#   INV-12  held only if the member delete rules REFUSE rather than cascade — ADR-005
#
# INV-10 IS STILL ABSENT AND IS STILL NOT ADDED BACK. The instruction above and
# `.ai/registry/features.md:152` both say so and both are registry plane. `01-story.md` complies and
# records, as Q-5, that compliance is not the same as agreement: by the test in `invariants.md` the
# shape HAD to be considered and was left at the seam, which that file says is evidence the invariant
# was in play. Routed to the steward; it changes nothing in this ticket either way.
invariants_touched: [INV-01, INV-02, INV-03, INV-04, INV-05, INV-06, INV-07, INV-08, INV-11, INV-12]
# ENUMERATED BY tech-lead-design AT DESIGN, 2026-08-26T09:22:00Z. `02-design.md` section 5 carries the
# per-entry justification; this list is the machine-readable copy that `scripts/check-allowed-paths.mjs`
# and review check R1 read.
#
# `prisma/**`, `prisma.config.ts` and `src/lib/data/prisma/**` are on the list IN ORDER TO BE REMOVED —
# the checker matches every changed path including deletions.
#
# `src/lib/auth/supabase.ts` is on the list because `.ai/registry/features.md:152` and this file's own
# header both require it: its comment block forbids naming the data package and ADR-007 reverses that.
# Comment text only; the code is untouched.
#
# `src/lib/data/types.ts`, `src/lib/data/mock/**` and `src/lib/data/fixtures.ts` are COMMENT TEXT ONLY.
# No type, no fixture value and no mock behaviour changes — AC-5 and AC-12 are the criteria and R5 is
# where it is judged.
#
# `tests/unit/self-signup.test.ts` was ADDED 2026-08-27T04:51:28Z by the DESIGN amendment that
# answers the QA FAIL. It took the list to 26 entries, from 25. It is the ONE path on this list that
# belongs to
# a ticket already DONE — SYS-01 — and the scope is TWO LINES: the SYS-01 AC-3 assertion that ADR-007
# clause 2 reverses. Nothing else in that file may be touched, and its SYS-01 AC-5 assertion is green
# today and must stay green. `02-design.md` section 5 carries the exact replacement text.
#
# SIX PATHS WERE ADDED 2026-08-28T02:06:22Z by the DESIGN amendment answering the second QA FAIL —
# `tests/db/**`, `tests/db-e2e/**`, `scripts/test-db.mjs`, `scripts/local-stack-client.ts`,
# `vitest.db.config.mts` and `playwright.db.config.ts`. It is 32 entries now, not 26. They are the
# `pnpm test:db` lane, which is what makes AC-1, AC-9, AC-10, AC-11 and AC-12 executable against the
# Supabase CLI's local stack without putting one test inside `pnpm test` or `pnpm test:e2e`.
# **THE SIX ARE NOT ALL ONE STAGE'S**: `02-design.md` 5.2 gives the runner, the two configs and the
# CI steps to the `developer`, and `tests/db/**` and `tests/db-e2e/**` to `qa` — and forbids the
# developer to write even a placeholder in those two directories, under RULE-05. Section 6.4 is the
# lane's full specification and is the part QA is dispatched with.
allowed_paths:
  - "supabase/**"
  - "src/lib/data/index.ts"
  - "src/lib/data/types.ts"
  - "src/lib/data/fixtures.ts"
  - "src/lib/data/mock/**"
  - "src/lib/data/prisma/**"
  - "src/lib/data/supabase/**"
  - "src/lib/auth/supabase.ts"
  - "src/app/(app)/seats/seats-manager.tsx"
  - "tests/unit/seam-parity.test.ts"
  - "tests/unit/self-signup.test.ts"
  - "tests/db/**"
  - "tests/db-e2e/**"
  - "scripts/seed.ts"
  - "scripts/test-db.mjs"
  - "scripts/local-stack-client.ts"
  - "scripts/check-docs.mjs"
  - "scripts/tests/check-docs.test.mjs"
  - "prisma/**"
  - "prisma.config.ts"
  - "eslint.config.mjs"
  - "vitest.config.mts"
  - "vitest.db.config.mts"
  - "playwright.db.config.ts"
  - "package.json"
  - "pnpm-lock.yaml"
  - ".env.example"
  - ".gitignore"
  - ".github/CODEOWNERS"
  - ".github/workflows/verify.yml"
  - "docker/Dockerfile"
  - "docker/docker-compose.yml"
# NOT none, and this is the one ticket where that is expected rather than a finding. The first
# migration creates the schema, adds Account.auth_user_id, and carries three constraints. RULE-09
# puts a human signature on it — that signature is the only stop in this ticket, and DESIGN should
# not raise it as a blocker.
schema_delta: "first migration — schema, Account.auth_user_id, INV-04/05/06 constraints"
requires_adr: false   # A DIFFERENT QUESTION FROM `adr:` BELOW, AND THE TWO DISAGREEING IS NOT A
                      # DEFECT. This asks whether the ticket still needs a decision taken; `adr:`
                      # names the decision already taken. Both are correct here: nothing is owed, and
                      # ADR-007 is what settled it.
# DoR item 4 reads this. Added 2026-08-26 by the orchestrator at the operator's instruction, after
# `/next-ticket` reported item 4 FAIL: `schema_delta` is not `none` and this field did not exist.
#
# NOTHING ABOUT THE DECISION CHANGED — ONLY THE FIELD THAT RECORDS IT. ADR-007 has been
# `ACCEPTED — 2026-08-25, by the operator` since before this ticket was seeded, and the SYS-02
# registry row already read "Implements ADR-007". What was missing was the machine-readable half:
# PR #54 added `adr:` to `.ai/templates/ticket.yaml` and rewrote DoR item 4 to read it, and every
# ticket in the repository predates that change, so none carries the field. SYS-02 is the only one
# that needs it, because it is the only ticket whose `schema_delta` is not `none`.
#
# This is a transcription, not an approval. RULE-09 still puts a human signature on the migration
# itself, and this ticket's own header says so — DESIGN is told not to raise that as a blocker.
adr: "ADR-007"
rework_count: 0
chat_budget:
  developer->tech-lead-design: { used: 0, max: 6 }
  developer->ba:               { used: 0, max: 6 }
  qa->ba:                      { used: 0, max: 6 }
  qa->tech-lead-design:        { used: 0, max: 6 }
tracker: { provider: clickup, external_id: "", sync_enabled: false }
gates:
  spec:   { passed: true,  at: "2026-08-27T07:56:34Z" }
  # RE-PASSED 2026-08-27T07:56:34Z ON THE THIRD RUN, the `R6` route out of `06-test-report.md`'s
  # `gate: FAIL` — *QA: AC ambiguous or untestable* -> `ba`, `increments_rework_count: false`.
  # `at:` moves because this row dates the story that is on disk. **The first PASS was
  # `2026-08-26T08:33:56Z`** and `01-story.md`'s changelog carries all three entries.
  #
  # WHAT CHANGED IS FIVE `Given` CLAUSES AND `Q-2`. AC-1, AC-9, AC-10, AC-11 and AC-12 now name the
  # database they are observed against — the Supabase CLI's local stack, which `ADR-007` OQ-2 already
  # decides and which needs no cloud credential. No `When`, no `Then`, no invariant and no
  # out-of-scope item was narrowed; out of scope GAINS item 12, forbidding any automated run from
  # reaching the live project. `01-story.md` §*The environment each criterion is observed in* is the
  # record and keeps the five previous Givens verbatim.
  #
  # `invariants_touched` AND `size_estimate` BELOW ARE UNCHANGED and were re-read rather than
  # re-derived. The scope did not move: the same criteria are asserted, against a named environment.
  #
  # `state:` ABOVE IS STALE AND THIS RUN DID NOT WRITE IT. It reads `REVIEW`; the ticket has since
  # been through QA, which returned FAIL. RULE-13 keeps `/review` and `/qa` out of this file and
  # `/spec` does not promote past its own gate, so the field has had no owner since MD-46 — the same
  # deadlock that left `REWORK` unwritten. The orchestrator is the writer.
  #
  # ONE CONSTRAINT DESIGN INHERITS, and it is what makes the QA gate satisfiable rather than merely
  # satisfied: the local stack needs a container runtime and the loop's machine has none, so the new
  # tests must live behind their own command and must NOT make `pnpm test` red where Docker is
  # absent. `allowed_paths` will need at least one more test path; that is DESIGN's to enumerate.
  design: { passed: true,  at: "2026-08-28T02:06:22Z" }
  # RE-AMENDED 2026-08-28T02:06:22Z, still PASS. All seven sections are still complete,
  # `allowed_paths` is enumerated above at 32 entries, and `size` is still `L`. `at:` moves because
  # this row dates the design that is on disk. **The first PASS was `2026-08-26T09:22:00Z`** and
  # `02-design.md`'s changelog carries all three entries.
  #
  # WHAT CHANGED IS ONE TEST LANE AND THE PATHS IT NEEDS. Section 6.4 specifies `pnpm test:db` —
  # runner, preflight, two configs, one database-handle module under `scripts/`, and the file each of
  # the five criteria goes in. 6.2 was rewritten so AC-9's regeneration is a NAMED TEST rather than a
  # workflow step, which is what the Definition of Done asks for. Sections 1, 2, 3 and 4 are
  # unchanged, so nothing about the contract, the permission model, the seam or the schema moved.
  #
  # ONE FINDING FOR THE STEWARD, `D-4`: the QA gate is worded twice and the two wordings disagree
  # about whether `pnpm test:db` is inside it — `.ai/01-operating-model.md:100` names the RUNNERS
  # ("vitest + playwright exit 0"), which reaches the lane; `.claude/commands/qa.md:46` names two
  # COMMANDS, which does not. 6.4 is built so both readings are safe, and under neither can the lane
  # make the gate falsely red. The residue is the command wording letting the gate go green while the
  # lane is red, with CI as the backstop. Neither file is in `allowed_paths`.
  #
  # ---- WHAT THIS BLOCK SAID BEFORE, KEPT ----
  # AMENDED 2026-08-27T04:51:28Z, still PASS. The amendment adds ONE path to `allowed_paths` —
  # `tests/unit/self-signup.test.ts` — and changes nothing else: all seven sections are still
  # complete and `size` is still `L`. `at:` moves to the amendment because this row dates the design
  # that is on disk, and the design on disk is the amended one. **The first PASS was
  # `2026-08-26T09:22:00Z`** and `02-design.md`'s changelog carries both entries.
  # PASS. All seven sections complete, `allowed_paths` enumerated above, `size` set to L.
  #
  # NOT BLOCKED, and the one thing that could have made it so is answered rather than deferred:
  # `schema_delta` is not `none`, and `requires_adr` is false because ADR-007 already took the
  # decision. `02-design.md` section 4 DRAFTS the first migration and does not approve it — the RULE-09
  # signature is the operator's, on the pull request, through CODEOWNERS. This file's own header
  # instructs DESIGN not to raise that as a blocker and it did not.
  #
  # ONE FINDING THAT DOES NOT BLOCK THIS GATE AND DOES BLOCK A GREEN PULL REQUEST — `D-1` in
  # `02-design.md`. Deleting `prisma/` and `src/lib/data/prisma/` breaks twelve path references in six
  # human-owned documents under `.ai/registry/**` and `.ai/standards/**`, which turns check D6 red;
  # `docs-audit` runs in CI on any pull request touching `.ai/`, and this one touches
  # `.ai/board/tickets/SYS-02/`. Those files are not in `allowed_paths` and must not be — the fix is a
  # steward change landing with this ticket. ADR-007's affected-documents table does not cover it
  # because `.ai/registry/decisions/**` is exempt from D6 (MD-38), which is why it was invisible when
  # that table was written.
  #
  # IN_PROGRESS HAS NO ROW IN THIS BLOCK AND THAT IS THE TEMPLATE'S SHAPE, NOT AN OMISSION.
  # `.ai/templates/ticket.yaml` declares exactly four gates — spec, design, review, qa — and the
  # IN_PROGRESS verdict lives in `03-impl-log.md`'s front-matter, which is where every stage's gate
  # is quoted from. `gate: PASS` there, 2026-08-27T01:28:07Z. `state:` above carries what it means.
  review: { passed: false, at: null }
  qa:     { passed: false, at: null }

# PRECONDITIONS — confirm before SPEC, stop if any is missing. ADR-007 records all three; they are
# repeated here so this ticket inherits them instead of discovering them.
#
#   1. DATABASE_URL, DIRECT_URL, the Supabase project URL and keys, and SUPABASE_SERVICE_ROLE_KEY are
#      present on the machine. .env.local is gitignored and no agent can verify it from the repo.
#      Under ADR-007 clause 7 the application does not start without them.
#
#      PARTIALLY MET AS OF 2026-08-26, VERIFIED BY READING KEY NAMES IN ALL THREE WORKTREES.
#      `.env.local` now exists in `aiw-design`, `aiw-implement` and `aiw-steward`, and each carries
#      `DATA_SOURCE`, `SUPABASE_URL`, `SUPABASE_ANON_KEY` and `SUPABASE_SERVICE_ROLE_KEY`.
#      **`DATABASE_URL` and `DIRECT_URL` are absent from all three.** Whether that blocks anything is
#      a real question rather than an oversight: `@supabase/supabase-js` authenticates with the URL
#      and a key and needs no Postgres connection string, so the two URLs are needed for the
#      MIGRATION half of this ticket and not for the client half. Precondition 3 says the two-URL
#      split *"is a pooler property and survives; the wiring does not"*. SPEC establishes which
#      tool runs the first migration and therefore which of the two URLs it needs — from the Supabase
#      CLI docs, per precondition 3, not from memory.
#   2. ANSWERED 2026-08-26 BY THE OPERATOR: THERE IS ONE SUPABASE PROJECT, NOT TWO. Their words:
#      *"CHỉ có 1 project supabase duy nhất."* This is a legitimate answer and not a violation —
#      ADR-007 §"Two Supabase projects, `dev` and `prod`" reserved exactly this to them: *"if a second
#      project costs money that is the one place the decision is theirs rather than the tech lead's."*
#      The ADR states the technical preference; the operator holds the call and has made it.
#
#      **THE CONSEQUENCE IS UNCHANGED BY THE ANSWER AND SPEC MUST CARRY IT.** Clause 7 defaults
#      `DATA_SOURCE` to `supabase`, so with one project every `pnpm dev` and every `pnpm verify`
#      reads and writes the same database the product uses. `.ai/standards/testing-standards.md`
#      depends on tests running without a network, and `06-test-report.md` for every future ticket
#      would be measured against live data.
#
#      **The lever already exists in the decision and SPEC does not need to invent one.** Clause 7:
#      *"The mock adapter is kept, not deleted — it is what lets unit tests run without a network."*
#      So `DATA_SOURCE` is the seam between the two, and what has to be decided is WHERE it is pinned
#      — per worktree in `.env.local`, in the test harness, or in CI — and whether the default
#      clause 7 flips is the default every lane actually runs under. That is a design question, not
#      an invention, and it belongs in `01-story.md` under OPEN QUESTIONS if SPEC cannot settle it
#      from what is written. It was NOT settled here.
#
#      ---- THE PRECONDITION THIS REPLACES, KEPT ----
#      2. Two Supabase projects exist, dev and prod. Decided 2026-08-26: clause 7 makes every
#         `pnpm dev` and every `pnpm verify` reach a database, so one shared project means every
#         test run touches production data.
#   3. .env.example still names Prisma's field layout. The two-URL split is a pooler property and
#      survives; the wiring does not. Verify against the Supabase CLI docs, not from memory.
#
# ALLOWED_PATHS MUST INCLUDE src/lib/auth/supabase.ts at DESIGN. Its comment block forbids naming the
# data package there and ADR-007 reverses that; RULE-03 refuses the edit if the path is left off.
#
# DO NOT SPLIT. D12 is red from the first commit until it is rewritten as a two-package map, and a
# split places a red pull request in the middle deliberately. MD-16's ten failing D12 tests are
# rewritten here, which is what MD-16 asked for.
