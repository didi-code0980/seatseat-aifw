---
doc_version: 1
last_updated: 2026-08-12
governed_by: [RULE-01]
---

# Model debt

Defects in the operating model itself, found while running it. Not ticket work — these are
gaps in the rules, the commands, or the guards.

Recorded here rather than fixed on discovery: patching the model mid-run makes it impossible
to tell whether a ticket succeeded because of the design or because of the patch.

Reviewed after each ticket closes.

| ID | Found | Defect | Severity | Fix shape |
|----|-------|--------|----------|-----------|
| MD-01 | ROO-01, DESIGN | ESCALATED has no exit command. The model says a human resolves it but not how. Resolved in prose. | Blocks the loop | `/resume <ID>` |
| MD-02 | ROO-01, DESIGN | D5 cannot distinguish a route (`/rooms`) from a slash command. Scoping removed the agent-facing false positives; the ambiguity remains for human-owned docs. | Cosmetic | Convention or allow-list — a decision, not an implementation |
| MD-03 | Bootstrap, Block 2 | Sessions carry no role identity. `agent_type` is absent, so `chat-guard` blocks every write addressed to a judging role rather than only the forbidden pairs. Sound, but broader than the rule. | Latent | Unknown — see below |
| MD-04 | ROO-01, DESIGN | `chat_budget` has no `tech-lead-design->ba` pair, though the topology permits that edge. | Gap | One line in the template |
| MD-05 | Steward setup | `01-operating-model.md:18` declares `.ai/standards/` to be registry plane; `guard-registry.mjs:86` blocks only `.ai/registry/**`. Agents have written standards freely all run. Either the document or the guard is wrong. | Real | A decision: tighten the guard, or correct the document |
| MD-06 | Steward setup | The registry is writable by one agent — **in intent only**. The intended control was a printed diff plus operator confirmation, which is weaker than a paste because a prompt shows a path, not content. The exemption was not implementable (see MD-03), so the steward pastes like everyone else and this risk is not yet live. | Accepted risk, not yet incurred | If MD-03 is ever fixed and the exemption lands, watch for confirmation fatigue; revert to print-for-paste if a registry change is approved that the operator had not read |
| MD-07 | ROO-01, `/ship`, 2026-08-23 | **`/ship` could not complete its own step 4.** It requires an open pull request; a pull request requires commits on a pushed branch; `.ai/standards/git-conventions.md` forbade agents to commit, and no step asked a human to. Raised by `orchestrator` when ROO-01 reached ship. Same shape as MD-01 — a step the model mandates and gives nobody a way to take. | Blocks the loop | **Resolved 2026-08-23.** A commit exception for `orchestrator` inside `/ship`, with the grouping left to its judgement and the branch boundary fixed by CI. **RULE-09 was not amended and no ADR was needed** — it names schema changes, ADRs, registry edits and merges, never commits. The prohibition lived in `.ai/standards/git-conventions.md` and was restated more broadly than RULE-09 reads in `CLAUDE.md` and `.claude/commands/ship.md`. All three amended; the registry was not touched. |
| MD-08 | Steward, 2026-08-23 | **RULE-03 is enforced per tool, not per action.** `guard-allowed-paths.mjs` is wired only to the `Edit\|Write` matcher, so a file write performed through `Bash` — `node -e`, a heredoc, `sed -i` — is never seen. Demonstrated accidentally and in one session: a write to `.ai/standards/git-conventions.md` via `node -e` succeeded, and the identical edit to `CLAUDE.md` via the `Edit` tool was blocked, on the same branch by the same caller. Every agent holding `Bash` (`developer`, `qa`, `devops`, `orchestrator`, `steward`) can write outside `allowed_paths` undetected. | Real | Undecided. Wiring the guard to `Bash` needs a command parser and would be guessing at shell grammar — the same class of mistake the settings metacharacter test already warns about. `scripts/check-allowed-paths.mjs` catches the result in CI, so the branch is protected even when the session is not; the gap is between the write and the push. |
| MD-09 | ROO-01 ship, 2026-08-23 | **`scripts/check-allowed-paths.mjs` is keyed on the branch name, so ticket work committed on a branch not named `feat/<ID>` is never checked.** The script exits 0 on any non-`feat/` branch by design — chore work has no ticket. ROO-01 was committed whole as `f5fd2e7` on `ops/orchestrator-commit-authority` and merged as PR #1, so the check took the "nothing to check" exit and RULE-03 was never enforced on the one diff it exists to police. This also voids MD-08's mitigation: the CI backstop only catches a stray write if the branch happens to be named after the ticket. | Real | Resolve the ticket from something the committer cannot rename — a `ticket:` line in the PR body, or the set of ticket folders whose state is not DONE — and refuse to pass vacuously when the diff touches `src/**` while no ticket resolves. |
| MD-10 | ADR-004, 2026-08-23 | **RULE-03 has no pre-write enforcement.** `guard-allowed-paths.mjs` is unwired, so no tool call is refused for writing outside the active ticket's `allowed_paths` — any agent can write any file in the repository at any time. What remains runs after the fact: review check R1, which depends on a reviewer reading carefully, and `scripts/check-allowed-paths.mjs` in CI, which MD-09 shows is skipped entirely on any branch not named `feat/<ID>`. The accepted cost of ADR-004, recorded here so it is a known price rather than a discovery. | Real | A decision: restore `guard-allowed-paths.mjs`, or close MD-09 so the CI check resolves its ticket from something the committer cannot rename and stops being skippable. Doing neither leaves RULE-03 as a convention. |
| MD-11 | SEA-01 setup, 2026-08-23 | **The WIP rule's parallel condition cannot be satisfied by two feature tickets.** `.ai/01-operating-model.md` permits parallel dispatch only when `allowed_paths` are "pairwise disjoint after glob expansion". `src/lib/data/types.ts` is in the `allowed_paths` of ROO-01, DEV-01 and MEM-01 — all three — because every feature adds DTOs to it, and SEA-01 will need it for the same reason. The condition is therefore unsatisfiable from DESIGN onward for exactly the tickets it was written to govern. It holds at SPEC, where `allowed_paths` is still `[]`. | Real | A decision. Either the condition is narrowed to *write* collisions that matter and `types.ts` is exempted as append-only, or parallel dispatch is honestly restricted to SPEC and the rule says so. Pretending the condition can be met is the worst of the three. |
| MD-12 | SEA-01 setup, 2026-08-23 | **A second worktree cannot be provisioned: `pnpm install` fails on this machine.** Prisma's `preinstall` script rejects Node v23.6.0, which is the only Node present — no nvm, fnm, volta or asdf, and Homebrew's is the same build. `package.json` declares `engines.node: >=20.9.0`, so the project accepts 23 and Prisma does not. The existing `aiw/node_modules` predates the constraint, which is why nothing surfaced until a second worktree was created. Worked around by symlinking `node_modules` from the first worktree; both share one lockfile so the dependency set is identical. | Real | Install a Node LTS (20 or 22) and a version manager, then each worktree installs its own. The symlink holds only while the branches share a lockfile, and nothing checks that they do. |
| MD-13 | ADR-006, 2026-08-24 | **`ADR-000-template.md` still describes the pre-ADR-004 flow and contradicts the standing instruction.** The template reads: *"An agent that needs a registry change sets `gate: BLOCKED` on its own artifact, states the requested decision in `blocking_reason`, and stops. A human writes the ADR. This is the intended flow, not a limitation to be routed around."* ADR-004 unwired `guard-registry.mjs`, and `.ai/steward/context.md` now instructs the steward to write `decisions/` when it is **recording** a decision the operator made in pointable words. Both statements are in force and they disagree about who types an ADR. ADR-006 was written under the standing instruction, not under the template. | Real | A decision: either the template absorbs the recording/authoring distinction the standing instruction already draws, or the standing instruction narrows. The distinction itself is sound and is what the template's own `## Status` test is built on — it is only the procedural paragraph that is stale. Registry path, RULE-01, so it is not fixed here. |

| MD-14 | ADR-006, 2026-08-24 | **INV-08 has no enforcement.** ADR-006 removed Better Auth, which held *"there is no self-signup"* with two controls in code — `disableSignUp: true` and the absence of a route. The operator chose to replace it with a client-side configuration flag held in `localStorage`, against the steward's recommendation of three controls. `localStorage` is browser storage: the value sits on the machine of the person it restrains and one line in a developer console changes it, with no server-side trace. It is **weaker than the Supabase dashboard toggle it replaces**, which at least sits on the provider's side of the network. INV-08 is currently a statement of intent, and `invariants.md` now says so at the invariant rather than only here — a reader of that table would otherwise assume a guarantee that does not exist. | Real | The third control from ADR-006's OQ-2 recommendation, which is the only one that survives someone changing a setting: a new audit check that fails if any client-side signup call or `signUp`-shaped export exists under `src/`. Roughly the size of D12's `src/**` branch, which already walks that tree. Pairs with creating users only through the Admin API from a server action gated by `canCreateAccount()`. Not built: the operator answered OQ-2 and did not ask for it, and adding an unasked-for gate to their own decision is not the steward's call. |

| MD-15 | SYS-01 seeding, 2026-08-24 | **A ticket parked between lanes strands its artifacts, and no agent is permitted to persist them.** `.ai/standards/session-model.md` says a feature may enter the build lane only when the previous one has merged, and the design lane may run as far ahead as the board allows. Both halves are sound and together they have no mechanism: the parked ticket's gated artifacts sit uncommitted in the design worktree, and `.ai/standards/git-conventions.md` permits a commit only from `orchestrator` inside `/ship` or on a direct operator instruction — neither of which describes parking. The next ticket cannot take the worktree, because a branch switch carries the modified and untracked files onto the new branch and `scripts/check-allowed-paths.mjs` diffs `origin/main...HEAD`. First hit by SEA-01: SPEC and DESIGN both PASS, roughly 90 KB of artifacts, and `aiw-work` blocked against SYS-01 until a human commits by hand. | **Resolved 2026-08-24**, hours after being recorded, by a better shape than the one proposed here. `/handoff` (`.claude/commands/handoff.md`) is an `orchestrator` command run at each lane boundary: it classifies the dirty tree against `allowed_paths`, commits the ticket set on `feat/<ID>`, pushes, sends anything else to an `ops/` branch, and then **releases the branch name** with `git switch --detach`. `.ai/standards/session-model.md` v4 carries the protocol; `/ship` moved to the design lane in the same change and is now the single writer of `metrics.md` and `backlog.md`. **What this entry got wrong, kept because a corrected register is less useful than a corrected one that says so:** the fix shape proposed below was a commit with *no push*, and it missed the step that actually matters. Git holds a branch name exclusively across worktrees, so persisting the artifacts is not sufficient — the next lane's `git switch` fails with `fatal: 'feat/<ID>' is already checked out at ...` until the name is freed. Committing without detaching would have moved the failure from the design lane to the build lane and made it harder to read, not easier. The original fix shape follows. | A third narrow commit exception, symmetrical with the `/ship` one and justified the same way — a step the model mandates that it gives nobody a way to take (the MD-01 and MD-07 shape). Likeliest form: the design lane commits its own gated artifacts to `feat/<ID>` at the point the ticket parks, with no push and no PR, so the assertion being made is only *these artifacts passed their gate*, which is exactly what the front-matter already claims. Alternatives are a fourth worktree per parked ticket, which does not scale, or requiring the operator to commit by hand every handoff, which is the thing they named as a standing cost. |

| MD-16 | Steward, 2026-08-24, while changing the lane flow | **`pnpm hooks:test` is red on `main`: ten D12 tests assert pre-ADR-006 semantics.** `scripts/check-docs.mjs` records `REWRITTEN 2026-08-24 for ADR-006` — until then D12 failed on ANY `@supabase/*` dependency; now `@supabase/ssr` is permitted, `@supabase/*` must be *named* in `no-restricted-imports`, and the check errors when the package is in the tree and the restriction is absent. The ten tests in `scripts/tests/check-docs.test.mjs` still assert the old contract — that a Supabase literal appearing in the restricted-pattern list is itself a finding — which under the new check is the opposite of a violation. Verified by execution, not by reading: `node --test` reports `# tests 175 / # pass 165 / # fail 10`, and `eslint.config.mjs`, `package.json` and `check-docs.mjs` are all clean in git, so nothing in the lane-flow change caused it. `node scripts/check-docs.mjs` itself exits 0 with no findings — the check works, its tests describe a different check. | Real, and louder than its severity: this is the suite that exists so a weakened guard cannot fail silently, and it is the guard on ADR-002's revert signal. A red suite is one a reader learns to skip, which is the MD-05 shape. | Rewrite the ten tests against the new contract, which is a decision about what each should now assert rather than a repair — the old intent (*a Supabase literal in the restrict list is a finding*) has no successor, because adding a restriction is now correct behaviour. Belongs to whoever landed ADR-006. **Do not delete them to make the suite green**, and do not relax D12 to match them: the check is the thing that works. |
| MD-17 | Steward, 2026-08-24, from the operator asking how to clear `gh auth` | **`/ship` step 7 mandates `gh pr create` and has failed on both ships that reached it, with no fallback written.** ROO-01: `gh` was not installed. DEV-01: `gh` 2.98.0 installed but no host logged in — `~/.config/gh/` does not exist, `GH_TOKEN` and `GITHUB_TOKEN` are unset. Both tickets finished `DONE` with an empty PR column and the next move left to a human to work out. The same shape as MD-01, MD-07 and MD-15: a step the model requires and gives nobody a way to take. Aggravated by `gh auth login` being an interactive TUI — run from a non-interactive session it blocks on stdin forever, which is what the operator hit. | Real, and the most-repeated one on this list — two occurrences out of two opportunities | **Fixed 2026-08-24** in `.claude/commands/ship.md` step 7: a prefilled `compare/main...feat/<ID>?expand=1&title=…&body=…` URL is now a first-class completion of the step rather than an improvisation, `gh auth status` is checked before either route, and `gh auth login` is explicitly forbidden to the command. **Not fixed, and not the loop's to fix:** authentication itself. The cheapest durable route is a fine-grained PAT (Contents RW, Pull requests RW, Metadata R) held in the macOS keychain and exported as `GH_TOKEN` from the shell profile — `gh` then needs no login at all, and every session inherits it because the Bash shell is initialised from the profile. Worth noting that `git push` already works unprompted via `credential.helper=osxkeychain`; `gh` does not read git's credential helper, which is the whole of why one works and the other does not. |
| MD-18 | Steward, 2026-08-24, from the operator asking whether `/spec` checks out its own branch | **No command cuts a ticket branch, and the one stage that could is the one that does not need to.** `ba` holds `Read, Grep, Glob, Write, Edit, SendMessage` and no `Bash`: it cannot run git, cannot read which branch is checked out, and writes `01-story.md` wherever HEAD happens to point without noticing. Neither `.claude/commands/spec.md` nor `.claude/commands/design.md` contained the word *branch* before this entry. The only ticket branch ever created was cut by `tech-lead-design` at DESIGN — recorded in `.ai/board/tickets/DEV-01/ticket.yaml:14`, in a header comment, as an improvisation rather than a step. Tolerable under one worktree, where everything stayed dirty until `/ship` classified it once. **The lane flow makes it a defect**: `/handoff` commits as soon as a lane finishes, so it needs the right branch beforehand, and it leaves the design lane on a *detached HEAD* — a story written there lands on a commit no branch reaches. Skipping the cut instead puts one ticket's story on the previous ticket's branch, where `/handoff` either exiles it to an `ops/` branch or `scripts/check-allowed-paths.mjs` fails the whole branch and blocks a human's merge. Nothing refuses the write at the time: `guard-allowed-paths.mjs` is unwired (ADR-004). | Real. Quiet in every direction, and the symptom appears one command away from the cause | **Resolved 2026-08-24, second attempt, and the first attempt is worth keeping.** It was a prose precondition in `spec.md` telling the operator to run `git switch -c` before dispatching the BA — a documented manual step, which is the class of fix that works until the day someone is in a hurry. The operator rejected it and asked for the branch handling to live inside `/spec`. That required granting `ba` the `Bash` tool, which it had never held. **Now:** `/spec` step 0 runs `pwd`, `git branch --show-current`, `git fetch`, `git status --porcelain`, then takes one of four paths — already correct, dirty tree (stop), branch exists (switch), branch absent (`git switch -c feat/<ID> origin/main`). Existence is checked against `refs/heads` and `refs/remotes` separately because a branch released by `/handoff` exists remotely while the worktree sits detached. `/handoff` no longer prints a branch-cut and deliberately leaves the lane detached, which is the state step 0 is written against. **Cut from `origin/main`, never local `main`** — nothing in this loop updates local `main` because no lane checks it out, so a branch cut from it silently omits everything merged since. **Still nothing enforces it**, and that is unchanged: a check refusing SPEC output on a non-`feat/<ID>` branch needs the restored `guard-allowed-paths.mjs` or role identity in the payload (MD-03). The cost of the fix is MD-19. |
| MD-19 | MEM-01 ship, 2026-08-24 | **The symlinked `node_modules` that provisioning prescribes makes Turbopack unusable, so the design lane can neither build nor run e2e — and `/ship` now lives in the design lane.** `.ai/standards/session-model.md` tells a secondary worktree to `ln -s /Users/mpa/Desktop/aiw/node_modules <folder>/node_modules`, which is the MD-12 workaround. Next 16's Turbopack rejects it outright: `Symlink [project]/node_modules is invalid, it points out of the filesystem root`, a `TurbopackInternalError` panic with a written panic log, not a warning. Both of `/ship`'s execution steps died on it — `pnpm verify` at `next build`, and `pnpm test:e2e` when Playwright's `webServer` tried to start the app — while `pnpm typecheck`, `pnpm lint` and `pnpm test` all passed through the same symlink, which is why this stayed invisible until a command needed a bundler. The workaround was written for the build lane, where `node_modules` is real; `/ship` moved to the design lane the same day and nobody re-ran it there. | Blocks the loop — `/ship` step 1 mandates `pnpm verify` and step 1 says any non-zero exit stops the ship | Repaired locally at MEM-01's ship with `pnpm install --frozen-lockfile --ignore-scripts`, which clears MD-12's Prisma `preinstall` refusal and is safe **only while nothing under `src/**` imports `@prisma/client`** — that is true today by decision, and `src/lib/data/prisma/client.ts:15` says so, but it ends the moment the schema is approved and the client is generated. Durable fix is MD-12's: a Node LTS and a version manager, then each worktree installs its own. Cheaper interim: make `--ignore-scripts` the documented provisioning step for a secondary worktree and delete the symlink instruction, noting that it breaks when Prisma's client is generated. Do not reach for `turbopack.root` in `next.config.ts` — it is a tracked file and a production build setting, and changing prod config to work around a local worktree layout is the wrong trade. |
| MD-20 | MEM-01 ship, 2026-08-24 | **`/ship` step 4 and step 6 contradict each other, and following step 4 produces a branch that branch protection refuses to merge.** Step 4 puts `backlog.md` and `metrics.md` in the ticket set, with a stated reason: *"a board that records a ship in a separate pull request from the ship is a board that can be merged out of order"*, and it acknowledges in the same breath that they *"sit outside every `allowed_paths`"*. Step 6 then runs `scripts/check-allowed-paths.mjs`, which diffs `origin/main...HEAD` and exempts only `.ai/board/tickets/<ID>/**` (`:113`), so it FAILS on precisely those two files — and step 6's own remedy is *"move that file to the second set, never widen the list"*, which is step 4 reversed. Hit on the first ship to run after the board writes moved into `/ship`; ROO-01 and DEV-01 never carried either file on their ticket branches, so the two halves had never been executed together. | Real, and self-inflicted at the point of merge: obeying step 4 blocks the human merge the whole command exists to hand over | A decision, and the ordering risk step 4 names is real so it should not just be dropped. Cheapest honest fix: exempt `.ai/board/backlog.md` and `.ai/board/metrics.md` in `check-allowed-paths.mjs` the way the ticket folder is already exempted, and delete step 6's remedy sentence for them — the board is the orchestrator's by the one-writer rule, not the ticket's, so it is not a RULE-03 widening. The alternative is to accept the split and make step 4 say so, which costs a merge-ordering instruction on every ship and gets it wrong once. Worked around at MEM-01 by reverting both files off `feat/MEM-01` in a second commit — `git push --force` is denied, so the first commit stands and the net diff is what CI reads — and moving them to `ops/board-mem-01-ship`, with the merge order stated in `backlog.md` and in both pull requests. |
| MD-21 | Steward, 2026-08-24, as the direct cost of fixing MD-18 | **Renumbered from MD-19 on 2026-08-24.** A parallel session recorded a different MD-19 and MD-20 from MEM-01's ship and merged first; `main` is the record, so these two moved. Both numbering collisions happened because two sessions appended to this table on the same afternoon with no allocator — worth noticing, not worth a mechanism yet. **`ba` now holds `Bash`, which walks around the one read guard still wired.** `guard-read-scope.mjs` is live on the `Read|Grep|Glob|NotebookEdit` matcher and refuses `ba` and `qa` any path under `src/**` — the mechanism behind RULE-05, and the only guard of the original set that both survived ADR-004 and names these roles. A shell reaches around it: `cat src/lib/data/types.ts` is not a `Read` call and the hook never sees it. Same shape as MD-08, which recorded the identical bypass for writes, but sharper — MD-08 described a guard that ADR-004 then unwired, while this one is running today. Granting the tool was the operator's instruction and it is the only way `/spec` can put itself on the right branch (MD-18); the exposure is the price, recorded so it is a known price rather than a discovery. | Real. Not yet incurred — no `ba` session has held `Bash` for a single ticket, so there is no evidence either way about whether the discipline holds | Three shapes, none free. **(a)** Wire `guard-read-scope.mjs` to `Bash` as well — needs a command parser and would be guessing at shell grammar, the objection that stopped the same fix for MD-08. **(b)** Give `ba` a narrower tool than `Bash` — no such tool exists in the harness. **(c)** Leave it to the agent definition, which is what is in force: `.claude/agents/ba.md` lists the seven git verbs `Bash` is for and forbids shell reads of `src/**` by name. That is a convention, and the register exists so nobody later mistakes it for a control. **Watch for**: any `01-story.md` whose `inputs_read` names a path under `src/**`, or whose acceptance criteria carry a field name the registry does not hold — both are the signature of a story written from the implementation, which is the failure RULE-05 exists to prevent. |
| MD-22 | Steward, 2026-08-24, adopting the branch-naming convention | **Renumbered from MD-20, same collision as MD-21.** **`bugfix/BUG_<FEATURE-ID>_<NN>` branches run with RULE-03 unenforced, at the moment it matters most.** Both resolvers hard-code the string `feat/`: `.claude/hooks/guard-allowed-paths.mjs` exits 0 on any branch that does not start with it, and `scripts/check-allowed-paths.mjs` prints *nothing to check* and exits 0. The convention adopted today names `bugfix/` as the branch for a defect on a shipped feature — so the one class of work that edits code already in `main`, under time pressure, is the class with no `allowed_paths` enforcement at write time and none in CI. `ops/` being exempt is correct and deliberate: chore work has no ticket. `bugfix/` has a ticket, and is exempt by accident of string matching. Compounds MD-09, which showed the CI check already skips whenever a committer renames a branch; this makes the skip reachable by following the convention rather than by breaking it. | Real. Not yet incurred — no `bugfix/` branch exists — which makes it the cheapest moment to fix it | Two files, and the change is a prefix table rather than new logic: both resolvers take the ticket ID from `feat/<ID>` **or** from `bugfix/BUG_<ID>_<NN>`, and treat every other prefix as chore work exactly as they do now. The regex is `^(?:feat/(?<id>[A-Z]{3}-\\d{2})\|bugfix/BUG_(?<id>[A-Z]{3}-\\d{2})_\\d{2})$`. Both files have tests, so the change is testable before it is trusted. **Do not widen it to any branch containing an ID** — `ops/fix-SYS-01-docs` is chore work and must stay exempt, and a resolver that greps for an ID anywhere in the name would capture it. |
| MD-23 | Steward, 2026-08-24, from the operator asking how to consult the BA | **A persistent role can be asked nothing, because every command is a stage.** `.ai/standards/session-model.md` keeps `ba` and `tech-lead-design` alive across a run for one stated reason — *roles that get asked stay alive* — and there is no command that asks them. The only way a session becomes the BA is `/spec <ID>`, which runs SPEC. Re-running it against a ticket whose gate has already passed is a state the model does not define; it happened to DEV-01 and `metrics.md` records the improvisation that absorbed it — the story was re-derived and judged rather than overwritten, and `gates.spec` was left at its original timestamp because *the gate was not re-passed, it was confirmed*. That worked, and it is a convention held by one metrics row. The human is also outside the chat topology entirely: RULE-11 says *agents* may chat and RULE-15 budgets *per pair*, so operator-to-agent conversation is unbounded and unrecorded — correct, but it means a discussion that changes a story depends wholly on RULE-14 being remembered. | Real. The same shape as MD-01, MD-07 and MD-15 — a thing the model requires and gives nobody a way to do | A read-only consult command: `/consult <agent> <ID>` loads the role with its artifacts and forbids it to write anything but an amendment under RULE-14, leaving `gates` untouched. Two decisions it needs first — whether a consult may amend at all or must route back through the owning stage, and whether the exchange is recorded in `99-questions.md` the way agent-to-agent clarification is. Both are the operator's. |
| MD-24 | Steward, 2026-08-24, same question | **`.ai/01-operating-model.md` still says only a human may write `features.md`, and that stopped being true four ADRs ago.** Line 40: *"A ticket cannot pass DoR unless its feature IDs already exist in `.ai/registry/features.md`, and only a human can put them there (RULE-01)."* ADR-004 unwired `guard-registry.mjs` and RULE-01 became a policy enforced by CODEOWNERS at merge time rather than a mechanism at write time; the steward has since written the MEM-01, SEA-01 and SYS-01 rows itself, and `.ai/steward/context.md` instructs it to. So the lifecycle document contradicts both the ADR and the standing instructions, in the one sentence a reader checks when they want to know who owns a feature row. | Real, and cheap. It misroutes exactly the question that found it — an operator reading line 40 concludes they must paste the row by hand, which is the friction ADR-004 was adopted to remove | One sentence, in a stop-and-ask file. It should read that a feature row may be written by an agent and is reviewed by CODEOWNERS on the pull request, with RULE-01's ADR requirement unchanged. Not done in the turn that found it: `.ai/01-operating-model.md` governs every agent's lifecycle and a correction to it deserves its own diff rather than arriving inside an answer to a question. |
| MD-25 | Steward, 2026-08-24, from a SEA-01 REVIEW reply the operator flagged | **The two roles the model most carefully isolates were told to advance the board they judge.** `.claude/commands/review.md` said *"On PASS, set the ticket to `QA`"* and `.claude/commands/qa.md` said *"On PASS, set the ticket to `DONE`"*. Both contradict the stage ownership table in `.ai/01-operating-model.md`, whose REVIEW row writes `04-review.md` and nothing else and whose QA row writes `tests/**`, `05-` and `06-` — `ticket.yaml` appears in neither, and `DONE` is the `orchestrator`'s at `/ship`. They also contradict `.claude/agents/orchestrator.md`, which owns *"`ticket.yaml` state transitions, from the returned artifact's front-matter"*. **The QA half is the worse of the two**: a QA session marking a ticket `DONE` skips the entire Definition of Done and the pull request, and the board would then claim a ship that never happened. The reviewer's behaviour was correct per its own command file, which is why this survived two full tickets — ROO-01 and DEV-01 both passed through it and the `orchestrator` happened to rewrite the same value afterwards, so the duplication was invisible. | Real. RULE-13 discards these two sessions after every verdict so the next pass starts cold; a verdict that executes itself has no separation left, and nothing between deciding and acting is reviewable | **Fixed 2026-08-24.** Both commands now write the verdict into their artifact's front-matter — `gate`, `blocking_reason`, `next_state` — and stop, with the reason stated rather than the rule cited. **Not fixed: nothing enforces it.** A judging session still holds `Write` and `Edit` and can reach `ticket.yaml`; the guard that would have refused it is unwired (MD-10). What would catch it after the fact is a check comparing a stage's writes against its ownership row — which does not exist, and would be the first audit check that reads the board rather than the documents. |
| MD-26 | Steward, 2026-08-24, after a `/qa` run signed off with nothing | **A rule defined once in `CLAUDE.md` was followed by five commands out of sixteen, because an agent follows the command file it is executing and stops where that file stops.** The sign-off block was written into `CLAUDE.md` `## Replying` and deliberately reproduced nowhere, on the reasoning that sixteen copies would drift. The reasoning was sound about drift and wrong about behaviour: `design.md`, `implement.md`, `next-ticket.md`, `sprint-status.md`, `status.md`, `docs-audit.md`, `pull-tickets.md`, `sync-tracker.md`, `thuki.md`, `idea.md` and `triage.md` never mentioned it, so a session running any of them ended at that file's last step. `qa.md` was worse than silent — it named the rule mid-file and then still ended with the pre-existing *print the next command and its session* block, so the two instructions contradicted each other and the older one was last. Same shape as MD-25, found the same day: **the command file beats the standard, every time, because it is what is being read.** | Real, and the general form matters more than this instance — any future rule placed only in `CLAUDE.md` inherits it | **Fixed 2026-08-24** by appending a `## Last step: sign off` section to all sixteen commands, in three variants: gated stages quote their gate, report commands end `gate n/a`, and `idea`/`triage` are told to write `unavailable — no Bash tool` because `product` cannot run `date`. **Not fixed: nothing checks it.** Sixteen copies of a pointer is exactly the drift the single definition was avoiding, and the answer is a `/docs-audit` check — every file in `.claude/commands/` contains the sign-off section — which is roughly ten lines in `scripts/check-docs.mjs` plus a test. Not added in the same turn: the D12 block of that suite is already red (MD-16), and adding an untested check to a suite nobody can currently run green would be adding a control that cannot be trusted. |
| MD-27 | Steward, 2026-08-25, from the operator reassigning `/handoff` | **Three documents asserted a capability limit that no agent definition imposes.** `.ai/standards/git-conventions.md`, `.ai/standards/session-model.md` and `.claude/commands/handoff.md` all stated that `ba` and `tech-lead-design` *hold no `Bash` tool and cannot commit*, and used it to justify routing both hand-offs through a separate `orchestrator` session. `grep '^tools:' .claude/agents/*.md` returns `Bash` for **every** role — `ba`, `tech-lead-design`, `qa`, `tech-lead-review`, `developer`, `orchestrator`. The claim may have been true when first written; nothing recorded it becoming false, and three documents repeated it downstream. **The defect is not the stale fact, it is that a rule was justified by a mechanism nobody re-checked** — the same shape as the RULE-09 belief corrected on 2026-08-23, where every document citing the rule agreed with each other and disagreed with the ledger. | **Corrected 2026-08-25** in all three, struck rather than deleted so the justification's failure stays visible. What has no fix yet: nothing checks that a claim about an agent's tools matches that agent's frontmatter. A check in the D-series could — the frontmatter is machine-readable and the assertions are greppable — and it would have caught this the day it went stale. |
| MD-28 | Steward, 2026-08-25, as the direct cost of MD-27's fix | **`/handoff` step 5 gives `qa` a wider commit surface than the rest of the `qa` role has.** Step 5 sends everything outside `allowed_paths` — model, registry, standards, hooks, scripts — to an `ops/` branch, and from 2026-08-25 the implement-lane hand-off is `qa`'s. That role is deliberately the narrowest in the model: RULE-13 discards it after every verdict and `guard-read-scope.mjs` refuses it `src/**`, yet it now commits `.ai/registry/**` and `.ai/standards/**` when a stage has left them dirty. Nothing is decided by the commit — recording is not authoring, and CODEOWNERS still reviews the `ops/` branch — so this is a widening, not a hole. Raised once before implementing and overruled; recorded because the residue outlives the conversation. | Two shapes, neither taken. Narrow step 5 for the `qa` hand-off to *stop and report* rather than commit, which costs a manual step exactly when a stage left the tree dirty. Or leave the second set to `/ship`, which already owns the board files and runs as `orchestrator` — at the cost of an `ops/` change sitting uncommitted across a lane boundary, which is the MD-15 failure returning by a smaller door. The command now says to stop and report rather than group a large or surprising second set into something coherent-looking. |
| MD-29 | SEA-01 ship, 2026-08-25 | **Renumbered from MD-23, which collided the same way MD-21 and MD-22 did — `ops/e2e-isolation-md-23` was never merged and `main` grew its own MD-23 and MD-24 meanwhile. That branch is superseded by this one.** Two defects, found when `/ship SEA-01` stopped at the Definition of Done. **(a) A gate can pass against a base the ticket will never merge into, and nothing re-checks it at ship.** `feat/SEA-01` was cut before MEM-01 merged and was 25 commits behind `origin/main` when `/ship` acquired it, so QA had run against a tree with neither `tests/e2e/members.spec.ts` nor `tests/unit/members.test.ts`: `06-test-report.md:23` records `e2e | exit 0 | 45 passed` and 74 unit, which are exactly the pre-MEM-01 counts against today's 61 and 92. The QA gate is `passed: true` and was honestly measured — of a tree that no longer exists. **(b) The e2e suite has no isolation between workers.** Merging `origin/main` at ship surfaced it immediately: `tests/e2e/members.spec.ts:477` (`AC-5`, *no other member is changed in any respect*) fails because `SEAT-B-06` moves from Ada to Mo mid-assertion. `playwright.config.ts` set `fullyParallel: true` with no `workers` cap, so SEA-01's seat-mutating spec ran in parallel with MEM-01's against **one `webServer` holding the mock store in process memory**. Confirmed by isolation, not by reading: `members.spec.ts` alone 16/16, `seats.spec.ts` + `members.spec.ts` 23/23, and the full suite failing the same test on three runs out of three. `06-test-report.md:65` claims *"Every E2E test restores the seat and device states it altered"* — true within a file, irrelevant across parallel workers. | **(b) blocked the loop** and was the first time it could fire at all: SEA-01 is the first ticket to land beside one that asserts non-interference. **(a) is the one that recurs** — every ticket cut before a merge carries a gate measured against a tree that has moved. | **(b) fixed 2026-08-25** in `playwright.config.ts`: `fullyParallel: false`, `workers: 1`, with the reason and the rejected alternative in a comment at the setting. Verified by execution on both trees — 61/61 exit 0 on `feat/SEA-01` where the parallel run had failed three times out of three, and 54/54 exit 0 on `main`. Costs 27s against ~15s. Per-worker store isolation (a server per worker, or a reset endpoint the fixture calls) is left open rather than rejected, and is what would make `fullyParallel` mean something again. Not a fix, and refused: weakening MEM-01's AC-5 assertion — *nothing else changed* is what INV-12 leans on. **(a) not fixed.** Fix shape: `/ship` step 0 asserts `git merge-base --is-ancestor origin/main HEAD` and stops rather than trusting the gate, which is what was done by hand here; or `/handoff` merges `origin/main` at each lane boundary so a gate is never measured against a stale base in the first place. The second is better and is a change to the lane protocol, so it is a decision rather than a repair. |


## Notes

**MD-01** is the only one that blocked work. Everything else was noticed, not hit.

**MD-03** is the one to be careful with. Three fixes are possible and they cost very different
amounts: an environment variable set when the session opens (certain, but requires opening
sessions from a terminal rather than editor tabs); a `.active-role` pointer written by each
stage command (cheap, works with editor tabs, correct only while WIP is 1); or leaving the
guard broad, which is what happens now. The right choice depends on whether mis-typed tabs
actually occur. Nobody knows yet.

**MD-03 now blocks something concrete.** It stopped being purely latent when the steward was created:
the registry exemption needs to be scoped to one agent, scoping needs role identity, and there is
none. `guard-registry.mjs` was left untouched rather than exempting every agent to reach one. That is
the whole of MD-06's "not yet incurred".

**MD-05 was verified, not assumed**, against `01-operating-model.md:18` and `guard-registry.mjs:86`.
Worth stating because it is the kind of claim that reads as obviously true and is cheap to check.

**MD-08 is narrower than it was recorded, and the correction is worth more than the original entry.**
The row above says any agent holding `Bash` can write outside `allowed_paths` undetected. For
`.ai/standards/**` and `.claude/**` that is true and was demonstrated. For `.ai/registry/**` it is
**not**: the harness refuses a `Bash` command that writes there before the command runs, independently
of `guard-registry.mjs`. Established by attempt on 2026-08-23 — the operator instructed the steward
to make an agreed registry edit directly, and both controls refused in turn, the project hook on the
`Edit` tool and the harness on the `Bash` route.

So the registry has two nets and everything else has one. That changes what MD-08 is worth fixing
for: not the registry, which is covered twice, but the standards and command planes, which are
covered once and only by a matcher that names two tool types.

It also settles a question the model had only ever asserted. `.ai/registry/**` being human-only is not
a convention the agents observe out of discipline — it survives an operator instructing an agent
three times to override it, which is a stronger property than anything written in `rules.md` can
claim on its own.

**A second register exists.** `.ai/board/model-defects.md` records overlapping defects under a
different numbering — `MD-1` is this file's `MD-01`, `MD-2` is `MD-04` — and additionally holds two
*resolved* entries with history absent here: the circular Definition of Ready, and the XL sizing row
that caught every ticket. Merging the two is an open decision for the operator. Until it is made,
**this file is the register of record** and `model-defects.md` is not being updated. Two registers
disagreeing is itself model debt; it is not listed as an MD item because recording it in one of the
two files would be the same mistake again.

**MD-08 and MD-09 are history as of 2026-08-23, not fixed.** ADR-004 unwired
`guard-allowed-paths.mjs`, so MD-08's complaint — that the guard is wired to `Edit|Write` and misses
`Bash` — describes a guard that no longer runs on any tool. MD-09's complaint about
`check-allowed-paths.mjs` passing vacuously on non-`feat/` branches still stands, and it now matters
more rather than less: with the hook gone, that script and review check R1 are the **only** things
enforcing RULE-03, and one of them is asleep on any branch not named after a ticket.

Both rows are kept rather than struck. If the guards are ever restored, both apply again unchanged,
and the record of what they cost is the cheapest argument for or against restoring them.

**MD-10 is the new one, and it is the price of ADR-004.** RULE-03 has no pre-write enforcement at
all. An agent can write any file in the repository at any time; nothing refuses. Review check R1
catches it if a reviewer reads carefully, and CI catches it only on a `feat/<ID>` branch. Severity is
**Real**, and the fix shape is a decision rather than an implementation: either restore
`guard-allowed-paths.mjs`, or fix MD-09 so the CI check resolves its ticket from something other than
the branch name and therefore stops being skippable.

## Review log

| Date | Ticket | Items closed | Items added |
|------|--------|--------------|-------------|
| 2026-08-23 | ROO-01 | MD-07 | MD-07, MD-08, MD-09 |
| 2026-08-23 | — | none | MD-10 |
| 2026-08-24 | SEA-01 / SYS-01 | MD-15 | MD-13, MD-14, MD-15, MD-16 |
| 2026-08-24 | — | MD-17 | MD-17 |
| 2026-08-24 | — | MD-18 | MD-18, MD-21 |
| 2026-08-24 | MEM-01 ship | none | MD-19, MD-20 |
| 2026-08-24 | — | none | MD-22 |
| 2026-08-24 | — | none | MD-23, MD-24 |
| 2026-08-24 | SEA-01 REVIEW | MD-25 | MD-25 |
| 2026-08-24 | SEA-01 QA | MD-26 | MD-26 |

**MD-07 opened and closed on the same day, and that is not tidiness.** It blocked the loop the moment
it was found, and the fix turned out to be three prose edits outside the registry — RULE-09 had never
forbidden what everyone believed it forbade. MD-08 and MD-09 were both found while fixing it, which
is the usual yield: a defect in a guard is easiest to see from inside the thing it failed to guard.
