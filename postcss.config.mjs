// Tailwind v4 is configured in CSS, not here. The theme lives in src/app/globals.css under
// @theme; there is no tailwind.config.js by design.
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
