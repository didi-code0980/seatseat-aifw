# Human review is required on every path where RULE-01 or RULE-09 applies.
#
# Owner resolved 2026-08-11. Confirm @didi-code0980 has push access to this repository before
# enabling branch protection — CODEOWNERS silently reviews nothing if the handle cannot be resolved
# or lacks write permission, and the failure mode is a rule that appears active and is not.

/.ai/registry/    @didi-code0980
/.ai/standards/   @didi-code0980
# RULE-09 lives here now. `supabase/migrations/` IS the schema since ADR-007, so this line is what
# puts a human signature on every schema change mechanically rather than by memory.
/supabase/        @didi-code0980
/.claude/         @didi-code0980
/.github/         @didi-code0980
/.mcp.json        @didi-code0980
